import React from "react";
import ReactDOM from "react-dom/client";
import NorgeKart from "./NorgeKart.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NorgeKart />
  </React.StrictMode>
);
